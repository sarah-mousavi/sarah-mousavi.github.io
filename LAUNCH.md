# چک‌لیست پیش از انتشار

وضعیت سایت: از نظر طراحی و محتوا آمادهٔ انتشار است. موارد زیر قبل از معرفی عمومی لینک باید انجام شود.

---

## ۱. مسدودکننده‌ها — بدون این‌ها لینک را منتشر نکنید

- [ ] **فرم رزرو هیچ‌جا ارسال نمی‌شود.**
      الان فقط اعتبارسنجی می‌کند و پیام موفقیت نشان می‌دهد؛ هیچ داده‌ای به کسی نمی‌رسد.
      محل اتصال: `src/components/Contact.astro`، بخش `TODO`.
      سریع‌ترین راه: یک فرم رایگان در Formspree و جایگزینی `TODO` با یک `fetch` به آدرس آن.
      تا آن زمان بهتر است فرم پنهان شود و فقط واتس‌اپ بماند — کاربری که فرم پر کند و جوابی نگیرد، از دست می‌رود.

- [ ] **تأیید نهایی محتوا توسط دکتر موسوی.**
      متن بخش‌های «درمان چیست؟»، «خدمات» و «پرسش‌های متداول» پیش‌نویس است و بر اساس
      موضوع پست‌های اینستاگرام نوشته شده، نه دیکتهٔ خودِ ایشان. هر ادعای بالینی باید تأیید شود.

- [ ] **مدارک و سوابق تحصیلی واقعی.**
      بخش «درباره من» عمداً کلی نوشته شده چون مدارک در دست نبود. برای اعتبار حرفه‌ای،
      مدرک تحصیلی، محل تحصیل و دوره‌های ISTDP باید صریح ذکر شود (`credentials` در `src/data/site.ts`).

- [ ] **بررسی شمارهٔ تماس.** `۰۹۱۲ ۲۷۹ ۴۶۰۶` — تأیید شود که همین شماره روی واتس‌اپ فعال است.
      تست: روی دکمهٔ واتس‌اپ کلیک کنید و ببینید چت درست باز می‌شود.

---

## ۲. مهم — بهتر است قبل از انتشار انجام شود

- [ ] **دامنهٔ اختصاصی.** لینک اینستاگرام ایشان به `sarahmousavi.ir` اشاره می‌کند.
      اگر دامنه در دسترس است، یک فایل `CNAME` در `public/` بگذارید و DNS را به GitHub Pages وصل کنید؛
      آدرس `sarah-mousavi.github.io` برای یک برند شخصی حرفه‌ای ایده‌آل نیست.
- [ ] **آدرس دقیق کلینیک** (و ترجیحاً لینک نقشه) در بخش تماس.
- [ ] **عکس محیط مطب.** کل سایت روی سه تصویر می‌چرخد. یک عکس از اتاق درمان
      بیشتر از هر تغییر بصری دیگری به حس اعتماد اضافه می‌کند.
- [ ] **تست روی موبایل واقعی** (نه فقط شبیه‌ساز) — مخاطب اصلی از اینستاگرام می‌آید.
- [ ] **تست سرعت از داخل ایران.** فونت‌ها self-host شده‌اند و هیچ درخواستی به Google Fonts
      نمی‌رود، ولی خودِ `github.io` باید از ایران بدون فیلترشکن بررسی شود.

---

## ۳. خوب است انجام شود

- [ ] معرفی سایت در بایو اینستاگرام و یک استوری اعلام.
- [ ] ثبت سایت در Google Search Console (`sitemap.xml` آماده است).
- [ ] چند پست دیگر برای بخش «آشناست؟» — اسکریپت تغییر رنگ روی هر پستی کار می‌کند.
- [ ] یک صفحهٔ ۴۰۴ ساده.
- [ ] بررسی متن هشدار بحران: شماره‌های ۱۲۳ و ۱۱۵ درست و به‌روز هستند.

---

## آنچه انجام شده

- طراحی و هویت بصری، تایپوگرافی فارسی (Amiri + Vazirmatn، هر دو self-host و SIL OFL)
- ساختار و محتوای همهٔ بخش‌ها، با لحن شخصی خودِ دکتر موسوی
- راست‌چین کامل، واکنش‌گرا از ۳۲۰ پیکسل به بالا، دکمهٔ ثابت رزرو روی موبایل
- دسترس‌پذیری: skip link، فوکوس قابل‌مشاهده، احترام به `prefers-reduced-motion`، برچسب برای همهٔ فیلدها
- SEO: متای فارسی، Open Graph، داده‌ساختاری `Psychologist` با شمارهٔ تماس، `robots.txt`، `sitemap.xml`
- انتشار خودکار روی GitHub Pages با هر push به `main`
